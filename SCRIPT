pipeline {
    agent any

    stages {
        stage('checkout') {
            steps {
                echo 'checkout step'
            }
        }
        
    
    stage('build') {
            steps {
                echo 'build step'
            }
    }
            stage('test') {
            steps {
                echo 'test job'
            }
            }
            stage('deploy') {
            steps {
                echo 'deploy job '
            }
}
}
}
